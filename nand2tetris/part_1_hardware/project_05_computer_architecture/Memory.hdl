// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/5/Memory.hdl
/**
 * The complete address space of the Hack computer's memory,
 * including RAM and memory-mapped I/O.
 * The chip facilitates read and write operations, as follows:
 *     Read:  out(t) = Memory[address(t)](t)
 *     Write: if load(t-1) then Memory[address(t-1)](t) = in(t-1)
 * In words: the chip always outputs the value stored at the memory
 * location specified by address. If load=1, the in value is loaded
 * into the memory location specified by address. This value becomes
 * available through the out output from the next time step onward.
 * Address space rules:
 * Only the upper 16K+8K+1 words of the Memory chip are used.
 * Access to address>0x6000 is invalid and reads 0. Access to any address
 * in the range 0x4000-0x5FFF results in accessing the screen memory
 * map. Access to address 0x6000 results in accessing the keyboard
 * memory map. The behavior in these addresses is described in the Screen
 * and Keyboard chip specifications given in the lectures and the book.
 */
CHIP Memory {
    IN in[16], load, address[15];
    OUT out[16];

    PARTS:
    // lowest 13 bits of address go to RAM16k if highest bit of address is 0
    Not(in=address[14] , out=loadram );
    And(a=load , b=loadram , out=writeram );
    RAM16K(in=in , load=writeram , address=address[0..13] , out=ram );

    // lowest 12 bits of address go to Screen if second-highest address bit is 0
    Not(in=address[13] , out=loadscreen );
    Not(in=loadram , out=notwritingram );
    And(a=load , b=notwritingram , out=oktowritescreen );
    And(a=oktowritescreen , b=loadscreen , out=writescreen );
    Screen(in=in , load=writescreen , address=address[0..12] , out=screen );

    // keyboard is just one read-only word
    Keyboard(out=keyboard );

    // highest bit of address is 0 if in RAM, 1 if in IO (Screen or Keyboard)
    // second-highest address bit is 0 if in Screen, 1 if at Keyboard
    Mux16(a=screen , b=keyboard , sel=address[13] , out=io );
    Mux16(a=ram , b=io , sel=address[14] , out=out );
}